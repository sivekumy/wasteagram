//Model class used to save data that is entered in each journal entry
class PostFields {
  int numberOfItems;
  int latitude;
  int longitude;

  PostFields({this.numberOfItems, this.latitude, this.longitude});
}
